<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="InventoryDialog" parent="@android:style/Theme.Holo.Dialog">
       <item name="android:windowBackground">@drawable/mapdesc</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="MapDescriptionDialog" parent="@android:style/Theme.Holo.Dialog">
        <item name="android:windowBackground">@drawable/mapdesc</item>
        <item name="android:windowNoTitle">true</item>
    </style>
    <style name="HealthTooLowActivity" parent="@android:style/Theme.Holo.Dialog">
        <item name="android:windowBackground">@drawable/mapdesc</item>
        <item name="android:windowNoTitle">true</item>
    </style>
    <style name="DiceDialog" parent="@android:style/Theme.Holo.Dialog">
        <item name="android:windowBackground">@drawable/dicetraycentered</item>
        <item name="android:windowNoTitle">true</item>
    </style>
    <style name="DiceRollResults" parent="@android:style/Theme.Holo.Dialog">
        <item name="android:windowBackground">@drawable/mapdesc</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>
